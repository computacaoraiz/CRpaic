.de URL
\\$2 \(laURL: \\$1 \(ra\\$3
..
.if \n[.g] .mso www.tmac
.TH CRPAIC_IS_ODD 3 "2025-07-24" "CRpaic" "CRpaic User's Manual"
.SH NAME
crpaic_is_odd - returns true if an integer argument is odd
.SH SYNOPSIS
.nf
.B #include <CRpaic.h>
.PP
.BI "bool crpaic_is_odd (" n ");"
.PP
.BI "bool crpaic_is_odd_c (char " n ")"
.BI "bool crpaic_is_odd_uc (unsigned char " n ")"
.BI "bool crpaic_is_odd_si (short int " n ")"
.BI "bool crpaic_is_odd_usi (unsigned short int " n ")"
.BI "bool crpaic_is_odd_i (int " n ")"
.BI "bool crpaic_is_odd_ui (unsigned int int " n ")"
.BI "bool crpaic_is_odd_li (long int " n ")"
.BI "bool crpaic_is_odd_uli (unsigned long int " n ")"
.BI "bool crpaic_is_odd_lli (long long int " n ")"
.BI "bool crpaic_is_odd_ulli (unsigned long long int " n ")"
.fi
.SH DESCRIPTION
The
.BR crpaic_is_odd ()
is a family of predicates that takes as an argument a number
.IR n
of any integer type and verify if the argument is odd. The user can pass the
following types of integers:
.PP
    a) Signed or unsigned standard integers: char, short int, int, long int,
       and long long int.
.PP
The
.BR crpaic_is_odd ()
predicate is a _Generic macro that dispatches the argument
.IR n
to the correct specific predicate depending on the data type of
.IR n
(the argument). The client program can directly use one of the specific
predicates to a given data type but, in general, it is recommended to use only
the generic
.BR crpaic_is_odd ()
macro in client programs.
.PP
Predicates specific to a given data type are indicated by a standardized suffix,
for example,
.BR crpaic_is_odd_si ()
takes a short int as argument;
.BR crpaic_is_odd_uli ()
takes an unsigned long int as argument. OK, I know that embedding the
argument\(aqs data type in the function name (know as \(dqHungarian
Notation\(dq) is stupid. However, due to the purely academic nature of the
CRpaic library and to the use of the generic dispatch macro that works for any
numeric integer data type, I don\(aqt see any major problems with this practice.
.SH RETURN VALUE
The
.BR crpaic_is_odd ()
predicate returns \(dqtrue\(dq if the argument
.IR n
is odd, and \(dqfalse\(dq otherwise.
.SH ERRORS
.BR crpaic_is_odd ()
does not set any
.IR errno
even in the event of an error.
.SH ATTRIBUTES
The
.BR crpaic_is_odd ()
is:
.TP
.BR Thread-Safe ?
Yes, this function is Multi Thread-Safe (MT-Safe).
.TP
.BR Reentrant ?
Yes, this function is reentrant.
.PP
For more explanation about these concepts, see
.BR attributes (7).
.SH CONFORMING TO
Not applicable.
.SH EXAMPLE
.in +4n
.EX
#include <CRpaic.h>
#include <stdbool.h>
#include <stdio.h>

int main (void)
{
    char n1 = -7;
    unsigned short int n2 = 7;
    int32_t n3 = -7;
    unsigned long int n4 = 7;
    long long int n5 = -7;

    if (crpaic_is_odd(n1))
        printf(\(dq%2d is odd.\\n\(dq, n1);
    else
        printf(\(dq%2d is even.\\n\(dq, n1);
    
    if (crpaic_is_odd(n2))
        printf(\(dq%2u is odd.\\n\(dq, n2);
    else
        printf(\(dq%2u is even.\\n\(dq, n2);

    if (crpaic_is_odd(n3))
        printf(\(dq%2d is odd.\\n\(dq, n3);
    else
        printf(\(dq%2d is even.\\n\(dq, n3);

    if (crpaic_is_odd(n4))
        printf(\(dq%2lu is odd.\\n\(dq, n4);
    else
        printf(\(dq%2lu is even.\\n\(dq, n4);

    if (crpaic_is_odd(n5))
        printf(\(dq%2lld is odd.\\n\(dq, n5);
    else
        printf(\(dq%2lld is even.\\n\(dq, n5);

    return 0;
}
.EE
.in
.SH APPLICATION USAGE
The argument
.IR n
can be of virtually any integer data types. Floating point arguments will cause
an compilation error. It's best to use the generic dispatch macro,
.BR crpaic_is_odd (),
instead of using the specific predicates.
.SH RATIONALE
Determining if a number is odd is a common operation in programming and,
therefore,
.BR crpaic_is_odd ()
can be used in a wide range of computational problems.
.SH BUGS
Please report bugs and other issues on
.URL "https://github.com/computacaoraiz/CRpaic/" "CRpaic's GitHub:"
.SH FUTURE DIRECTIONS
None.
.SH SEE ALSO
.BR crpaic_is_even ()
.SH CHANGE HISTORY
First released in CRpaic version 2.0.0.
.SH COLOPHON
This page is part of CRpaic User\(aqs Manual
.IR man-pages .
A description of the project, information about reporting bugs, and the latest
version of this page, can be found at
.URL "https://github.com/computacaoraiz/CRpaic/" "CRpaic's GitHub".
